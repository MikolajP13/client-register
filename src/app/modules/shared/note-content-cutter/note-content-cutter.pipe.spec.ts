import { NoteContentCutterPipe } from './note-content-cutter.pipe';

describe('NoteContentCutterPipe', () => {
  it('create an instance', () => {
    const pipe = new NoteContentCutterPipe();
    expect(pipe).toBeTruthy();
  });
});
